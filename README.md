# Wealth of Nations — Clean Start

This repository has been reset to a minimal scaffold for a fresh project.

Files included:
- `README.md` (this file)
- `.gitignore` (see below)
- `data/`, `scripts/`, `outputs/` (empty, with .gitkeep files)

To continue, add your new dataset to `data/` and put scripts in `scripts/`.

